<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Peter.mapper.CommentMapper">
   <insert id="addComment" parameterType="com.Peter.entity.CommentEntity">
       insert into comment(
         module,
         user_id,
         resource_id,
         content,
         status,
         score,
         like_num,
         is_delete,
         create_time,
         update_time
         )
       values(
     #{module},
     #{userId},
     #{resourceId},
     #{content},
     #{status},
     #{score},
     #{likeNum},
     #{isDelete},
     #{createTime},
     #{updateTime}
       )
   </insert>

   <delete id="deleteCommentById" parameterType="java.lang.Long">
      delete from comment where id=#{id}
   </delete>

    <select id="queryCommentByParam" resultType="com.Peter.entity.CommentEntity" parameterType="com.Peter.entity.CommentParam">
       select * from comment
        <where>
            <if test="module != null">
                and module=#{module}
            </if>
            <if test="resourceId != null">
                and resource_id=#{resourceId}
            </if>
            <if test="score != null">
                and score=#{score}
            </if>
        </where>
        <if test="orderBy!= null and orderBy!='">
            order by ${orderBy}
            <if test="orderDirection!=null and orderDirection!='"> --不是空字符串
                ${orderDirection}
            </if>--按照排列方式逆序或顺序排列
        </if>
   </select>

    <select id="countCommentCountByParam" resultType="java.lang.Integer"  parameterType="com.Peter.entity.CommentParam">
       select count(*) from comment
       <where>
          <if test="module != null">
              and module=#{module}
          </if>
          <if test="resourceId != null">
               and resource_id=#{resourceId}
          </if>
           <if test="score != null">
               and score=#{score}
           </if>

       </where>
   </select>
</mapper>